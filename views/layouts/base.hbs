<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <!-- For linking a stylesheet in hbs, as long as it's in the base, anything that extends base can use it! -->
    <link rel="stylesheet" href="../../assets/basestyles.css">
</head>
<body>
    <div id="mainContainer" class="container">
        <!-- navbar for easier navigation -->
        <nav class="navbar navbar-expand-lg navbar-light bg-light mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">PokePort Admin Dashboard</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/cards">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/users/login">Login</a>
                        </li>
                    </ul>

                    <!-- Display the user currently logged in, otherwise ask them to log in-->
                     <div class="sm-mb-3">
                        {{#if user}}
                            Good to see you, <span class="user">{{user.username}}</span>!
                            <a href="/users/profile" class="mx-2 btn btn-primary btn-sm buttonAnimations">Profile</a>
                            <a href="/order" class="mx-2 btn btn-primary btn-sm buttonAnimation">Orders</a>
                            <a href="/cart" class="mx-2 btn btn-primary btn-sm buttonAnimations">Cart</a>
                            <a href="/users/logout" class="me-2 btn btn-primary btn-sm buttonAnimations">Logout</a>
                        {{else}}
                            Welcome, stranger! Please sign in:
                            <a href="/users/login" class="mx-2 btn btn-primary btn-sm buttonAnimations">Login</a>
                        {{/if}}
                    </div>

                    <form class="d-flex">
                        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-outline-success buttonAnimations" type="submit">Search</button>
                    </form>
                </div>
            </div>
        </nav>

        <!-- transfer flash messages from the session to the HBS file -->
        <!-- run through success messages and error messages, and display them if they exist -->
        {{#if success_messages }}
            {{#each success_messages}}
                <div class="alert alert-success">
                    {{this}}
                </div>
            {{/each}}
        {{/if}}

        {{#if error_messages }}
            {{#each error_messages}}
                <div class="alert alert-danger">
                    {{this}}
                </div>
            {{/each}}
        {{/if}}

        <div class="overflow-scroll">
            {{#block "content"}}
            {{/block}}
        </div>
    </div>
    <!-- bootstrap Script -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>

    <!-- JavaScript block -->
    {{#block "js"}}
    {{/block}}
</body>
</html>